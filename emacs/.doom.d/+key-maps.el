;;; ../Dotfiles/emacs/.doom.d/+key-maps.el -*- lexical-binding: t; -*-

(use-package! which-key
  :config
  (setq which-key-idle-delay 0.1)
  (setq which-key-max-description-length nil)
  (which-key-setup-minibuffer)
  )
